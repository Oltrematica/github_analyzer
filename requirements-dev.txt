# Development dependencies
#
# Security Note (Feature 006, FR-003):
# Development dependencies use bounded ranges to allow updates
# while maintaining compatibility. Runtime dependencies (requirements.txt)
# use exact pins for security.

# Include production dependencies
-r requirements.txt

# Testing
pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<6.0.0

# Linting and formatting
ruff>=0.1.0,<1.0.0

# Type checking (optional)
mypy>=1.0.0,<2.0.0
