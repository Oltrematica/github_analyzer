# Runtime dependencies for GitHub Analyzer
# Core functionality works with Python standard library only
#
# Security Note (Feature 006, FR-003):
# All runtime dependencies are pinned to exact versions for:
# - Reproducible builds
# - Security audits (pip-audit can analyze specific versions)
# - Supply chain security

# HTTP requests (optional - falls back to urllib)
requests==2.31.0
